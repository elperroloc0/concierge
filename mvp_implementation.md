Ок — разбиваю MVP на микрошаги так, чтобы их можно было делать по одному (и отмечать ✅). Это последовательность, которая ведёт к “первому рабочему звонку → запись в админке → SMS ушла”.

Принципы микрошагов
	•	каждый шаг = маленький коммит
	•	всегда после шага проект остаётся runnable
	•	сначала каркас и данные, потом webhooks, потом SMS, потом polish

⸻

MVP Micro-Steps (Django)

0. Репозиторий и базовый каркас

0.1 Создать репозиторий
	•	git init
	•	.gitignore (Python/Django, .env, pycache, media, static)
	•	README.md (1 абзац: что это, как запустить локально)

0.2 Подготовить окружение
	•	pyproject.toml / requirements.txt
	•	Django
	•	psycopg
	•	celery
	•	redis
	•	python-dotenv или django-environ
	•	twilio
	•	python -m venv .venv / poetry
	•	pip install -r requirements.txt

0.3 Создать Django проект
	•	django-admin startproject config .
	•	python manage.py runserver работает

✅ чек: открывается стандартная стартовая страница Django.

⸻

1. Docker и конфиги (чтобы “как в проде”)

1.1 Добавить .env.example
	•	SECRET_KEY=
	•	DEBUG=1
	•	DATABASE_URL=
	•	REDIS_URL=
	•	RETELL_WEBHOOK_SECRET=
	•	TWILIO_ACCOUNT_SID=
	•	TWILIO_AUTH_TOKEN=
	•	TWILIO_FROM_NUMBER=

1.2 Настроить settings для env
	•	подключить django-environ (или dotenv)
	•	чтение DATABASE_URL
	•	чтение REDIS_URL
	•	настроить ALLOWED_HOSTS, CSRF_TRUSTED_ORIGINS (пока минимально)

1.3 Docker Compose (dev)
	•	docker-compose.yml:
	•	postgres
	•	redis
	•	локально Django подключается к postgres из compose

✅ чек: python manage.py migrate проходит на Postgres.

⸻

2. Разделение на приложения (apps)

2.1 Создать apps
	•	apps/tenants
	•	apps/calls
	•	apps/leads
	•	apps/messaging
	•	apps/webhooks
	•	добавить в INSTALLED_APPS

✅ чек: python manage.py check без ошибок.

⸻

3. Модели данных (минимум) + миграции

3.1 Restaurant модель
	•	Restaurant:
	•	name, timezone, language
	•	after_hours_only bool
	•	hours_json JSONField blank
	•	reservation_link URLField blank
	•	retell_agent_id CharField db_index
	•	twilio_phone_number CharField blank
	•	send_sms_recap_enabled bool
	•	send_reservation_link_enabled bool

3.2 Call модель
	•	Call:
	•	restaurant FK
	•	retell_call_id unique
	•	from_number, to_number
	•	started_at, ended_at, duration_sec
	•	intent (choices)
	•	transcript TextField blank
	•	raw_payload JSONField

3.3 Lead модель
	•	Lead:
	•	restaurant FK
	•	call OneToOne (или unique constraint)
	•	name blank
	•	phone
	•	intent
	•	party_size null
	•	requested_datetime null
	•	status (new/handled)

3.4 Message модель
	•	Message:
	•	restaurant FK
	•	lead FK null
	•	to_number
	•	body
	•	twilio_sid blank + index
	•	status choices (queued/sent/failed/delivered)
	•	error blank

3.5 Миграции
	•	makemigrations + migrate

✅ чек: все таблицы созданы, миграции чистые.

⸻

4. Django Admin (чтобы MVP уже “был”)

4.1 Зарегистрировать модели в admin
	•	RestaurantAdmin: list_display, search, filters
	•	CallAdmin: list_display (date/from/intent/duration), search by from_number, filter by intent
	•	LeadAdmin: list_display (phone/intent/status), filters
	•	MessageAdmin: list_display (to/status/sid), filters

4.2 Создать суперпользователя
	•	createsuperuser

✅ чек: заходишь в /admin/, можешь создать Restaurant вручную.

⸻

5. Celery подключение (без задач пока)

5.1 Добавить celery.py
	•	config/celery.py + автодискавер задач

5.2 Настроить брокер Redis
	•	CELERY_BROKER_URL из env
	•	минимальные настройки сериализации

5.3 Команды запуска
	•	в README: как запустить worker

✅ чек: celery -A config worker -l info стартует без ошибок.

⸻

6. Webhook Retell — скелет

6.1 URLConf
	•	webhooks/urls.py + подключить в config/urls.py
	•	endpoint POST /webhooks/retell/

6.2 View-заглушка
	•	принимает JSON
	•	проверяет secret header/token (пока простая проверка)
	•	возвращает 200

✅ чек: curl на endpoint возвращает 200, без записи в базу.

⸻

7. Retell payload parser (отдельный модуль)

7.1 Создать common/services/retell_parser.py
	•	функция parse_retell_payload(payload) -> dict:
	•	retell_call_id
	•	agent_id (если есть)
	•	from_number / to_number
	•	start/end/duration
	•	transcript
	•	intent (если есть)
	•	extracted fields (name, party_size, requested_time) если есть

7.2 Тесты на parser
	•	2–3 фикстуры payload (минимальные)

✅ чек: pytest (или Django test) проходит.

⸻

8. Webhook Retell — запись Call (idempotent)

8.1 Маппинг payload → Restaurant
	•	найти Restaurant по retell_agent_id == agent_id
	•	если не найден — лог + 200 (чтобы не зацикливать ретраи)

8.2 Upsert Call
	•	Call.objects.update_or_create(retell_call_id=..., defaults=...)
	•	transcript обновлять только если пришёл не пустой (и если текущий пустой — тоже)

✅ чек: 2 одинаковых webhook → в базе 1 Call.

⸻

9. Создание Lead (1 на Call)

9.1 Логика
	•	если есть from_number → создать Lead на Call (OneToOne)
	•	intent взять из payload или Call.intent
	•	name/party_size/requested_datetime — если есть

✅ чек: после webhook есть Call + Lead.

⸻

10. Постановка SMS в очередь (без отправки)

10.1 Factory сообщений
	•	messaging/services.py:
	•	build_recap_sms(restaurant, lead, call) -> str
	•	build_reservation_sms(restaurant, lead) -> str

10.2 Создание Message записей
	•	если restaurant.send_sms_recap_enabled → Message(status=queued)
	•	если intent=reservation и send_reservation_link_enabled и reservation_link есть → Message(status=queued)

✅ чек: после webhook появляются Message(queued), но ещё не отправляются.


Продолжаю с шага 11 — доводим до “реально ушёл SMS”, потом делаем устойчивость и минимальные метрики.

⸻

MVP Micro-Steps (Django) — продолжение

11. TwilioService (адаптер)

11.1 Создать сервис-обертку
	•	common/services/twilio_service.py
	•	инициализация клиента из TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN
	•	метод send_sms(to_number: str, body: str, from_number: str) -> (sid, status)

Архит. решение: “adapter layer” — чтобы позже легко заменить Twilio (Telnyx/Plivo) без переписывания бизнес-логики.

11.2 Нормализация телефонов
	•	common/utils/phone.py:
	•	функция normalize_e164(phone: str) -> str (минимум: trim, оставить +, убрать пробелы/скобки)
	•	(в MVP можно без phonenumbers, но лучше подключить библиотеку, если хочешь точнее)

✅ чек: можно вызвать TwilioService из Django shell и отправить тестовый SMS.

⸻

12. Celery task: отправка SMS

12.1 Task send_message
	•	apps/messaging/tasks.py
	•	@shared_task(bind=True, max_retries=3)
	•	читает Message по id
	•	если status уже sent/delivered → return (идемпотентность)
	•	вызывает TwilioService.send_sms(...)
	•	сохраняет twilio_sid, ставит status=sent (или статус от Twilio)
	•	при исключении → retry(countdown=...), при финальном фейле → status=failed, error=...

Архит. решение: идемпотентность на уровне Message + Celery retries. Это защищает от дублей и временных ошибок сети.

12.2 Связать webhook → enqueue

В обработчике /webhooks/retell/ после создания Message(status=queued):
	•	send_message.delay(message.id) (или apply_async)

✅ чек: после webhook queued → через секунды sent, в админке видно sid.

⸻

13. Twilio status callback (delivery updates) — рекомендовано

13.1 Endpoint
	•	POST /webhooks/twilio/status/
	•	принять поля MessageSid, MessageStatus (Twilio присылает)
	•	найти Message по twilio_sid
	•	обновить status на delivered/failed и т.п.

13.2 Security (минимум)
	•	проверить подпись Twilio webhook (Twilio request validation)
	•	в MVP можно начать без, но лучше включить сразу

✅ чек: при доставке SMS статус в базе обновляется на delivered (если оператор/регион поддерживает).

⸻

14. Smoke test end-to-end (реальный)

14.1 Тестовый ресторан (ручной)
	•	Создать Restaurant:
	•	retell_agent_id
	•	send_sms_recap_enabled=true
	•	send_reservation_link_enabled=true
	•	reservation_link=...
	•	timezone=America/New_York
	•	language=es (или en)

14.2 Тест webhook payload
	•	отправить тестовый Retell payload через curl/postman (с reservation intent)
	•	проверить:
	•	Call создан
	•	Lead создан
	•	Message(queued) → Message(sent)
	•	SMS реально пришёл

✅ чек: “один звонок” полностью обработан системой.

⸻

15. Жёсткая идемпотентность и защита от дублей

15.1 Уникальности в БД
	•	Call.retell_call_id UNIQUE (уже)
	•	Lead.call OneToOne (уже)
	•	(опционально) Message уникальность на пару:
	•	unique_together(lead, purpose) где purpose = recap/reservation
	•	или хранить message_type и делать unique по (lead, message_type)

15.2 Логика “не отправлять дважды”
	•	перед созданием Message проверять: нет ли уже Message такого типа для этого Lead

✅ чек: двойной webhook НЕ создаёт вторые SMS.

⸻

16. Улучшение Retell parser (минимальные полезные поля)

16.1 intent mapping
	•	map_intent(raw) -> one_of(reservation/info/other/unknown)
	•	если есть ключевые слова (“reserve”, “booking”, “table”) — fallback классификация (очень простая)

16.2 извлечение party_size и datetime (если Retell присылает)
	•	сохранять как есть в Lead.party_size, Lead.requested_datetime (timezone ресторана)

✅ чек: для “Quiero una mesa para 2 a las 7” в Lead появляются поля (если Retell присылает).

⸻

17. Admin usability (быстрые улучшения)

17.1 Удобный просмотр transcript
	•	в CallAdmin: добавь поле transcript_preview (первые 200 символов)
	•	в detail view: transcript целиком, raw_payload в read-only

17.2 Фильтры
	•	Calls: filter by restaurant, intent, date (DateHierarchy)
	•	Leads: filter by status, intent, restaurant
	•	Messages: filter by status, type

✅ чек: по админке можно реально работать.

⸻

18. Мини-метрики (без отдельного dashboard)

18.1 Admin “статистика ресторана”
	•	в RestaurantAdmin добавить readonly поля:
	•	calls_last_7d
	•	reservation_intents_last_7d
	•	avg_duration_last_7d

Реализовать как методы admin/model.

✅ чек: владелец/ты видит базовую ценность сразу.

⸻

19. Logging + correlation ids

19.1 Структурные логи
	•	настроить логгер с JSON или хотя бы единый формат
	•	в webhook логах всегда писать:
	•	retell_call_id
	•	restaurant_id
	•	event_type

19.2 Маскирование PII
	•	утилита mask_phone("+1305...") -> "+1***1234"
	•	использовать в логах

✅ чек: по логам легко дебажить, но без утечек телефонов.

⸻

20. Error handling + dead letters (упрощенно)

20.1 Таблица для “неразобранных webhook”
	•	модель WebhookEvent (опционально, но полезно)
	•	source=retell
	•	received_at
	•	payload
	•	error
	•	processed bool

Или проще: сохранять raw_payload в Call и логировать исключения.

20.2 Celery failure visibility
	•	если Message failed — видно error в админке

✅ чек: если Twilio падает/неверный номер — это видно и не ломает систему.

⸻

21. Deployment readiness (минимум)

21.1 Команды запуска
	•	./manage.py migrate
	•	./manage.py createsuperuser
	•	gunicorn config.wsgi:application
	•	celery -A config worker -l info

21.2 Healthcheck
	•	endpoint GET /health/ → 200 + версия

✅ чек: можно поднять на сервере без сюрпризов.

⸻

22. Definition of Done (MVP финал)
	•	Реальный звонок обработан: Call + transcript сохранены
	•	Lead создан (телефон, intent)
	•	SMS recap отправлен (Twilio), статус отражается
	•	Reservation link SMS отправлен при intent reservation
	•	Webhook повторно не создаёт дубликаты и не шлёт повторные SMS
	•	Админка удобна: поиск/фильтры/просмотр transcript
	•	Логи пригодны для дебага, PII замаскировано

⸻
Да — если ты реально сделаешь шаги 1–22 именно так (multi-tenant, idempotency, Celery, adapter’ы), этот MVP будет нормально расширяемым на десятки/сотни ресторанов. Но есть 5 мест, где обычно “MVP внезапно перестаёт масштабироваться”. Я перечислю их и что нужно добавить/проверить, чтобы масштаб был лёгким.

Что уже делает MVP масштабируемым
	•	Одна БД + restaurant_id везде → добавление ресторана = запись в таблице.
	•	Webhook → быстрая запись → тяжёлое в Celery → рост трафика не валит web.
	•	Idempotency по retell_call_id → ретраи не создают хаос.
	•	Twilio через твой backend → ты контролируешь стоимость и каналы.
	•	Сервисный слой (Retell parser / TwilioService) → легко добавлять провайдеры/фичи.

5 “точек боли” при росте и что сделать

1) Определение ресторана в webhook (маппинг)

Если ты маппишь ресторан только по retell_agent_id, то масштабировать легко при условии, что:
	•	каждому ресторану закреплён свой agent_id (или чёткая схема “template + vars”)

Что добавить:
	•	таблицу маппинга InboundRoute:
	•	retell_agent_id
	•	twilio_number / to_number
	•	restaurant_id
Это спасает, когда появятся разные каналы/номера.

✅ Проверка: можно добавить ресторан без изменений кода — только через админку.

⸻

2) Очереди задач (Celery) и rate limits

Когда ресторанов много — SMS могут “пачками” уходить.

Что добавить:
	•	разделить очереди:
	•	critical (webhook follow-ups)
	•	default (отчёты/прочее)
	•	ограничение скорости на отправку (rate limit) или хотя бы retries/backoff
	•	хранить message_type и дедупликацию

✅ Проверка: 100 webhook за минуту не кладут worker и не создают дубликаты SMS.

⸻

3) Индексы и выборки в админке

При росте Call/Message таблицы раздуются.

Что добавить (обязательно при первых 10+ ресторанах):
	•	индексы:
	•	Call(restaurant_id, started_at)
	•	Call(intent, started_at)
	•	Lead(restaurant_id, created_at)
	•	Message(restaurant_id, created_at)
	•	пагинация и ограничение выборок в админке (Django Admin иногда грузит тяжело)

✅ Проверка: список Calls за неделю открывается быстро.

⸻

4) Настройки “на ресторан” (переменные агента)

Чтобы не плодить 200 промптов:
	•	делай agent template + variables
	•	в БД храни:
	•	address, hours, cuisine, policies, reservation_link
	•	Retell либо использует переменные (если поддерживает), либо ты формируешь компактный prompt из этих данных

✅ Проверка: новый ресторан = заполнить поля, не переписывая агент руками.

⸻

5) Наблюдаемость и стоимость

Масштабирование ломается не от багов, а от того, что ты не видишь:
	•	кто тратит больше минут Retell
	•	где падают webhooks
	•	где не доставляются SMS

Что добавить минимально:
	•	метрики по ресторану:
	•	calls/day
	•	avg duration
	•	sms sent/failed
	•	алерт: spike ошибок webhooks / Twilio failures

✅ Проверка: ты можешь понять “почему этот ресторан дорогой/проблемный” за 5 минут.

⸻

Итог

После этих шагов MVP масштабируется по “операционке”: добавление ресторана — это настройка в админке + привязка номера/агента.

Но чтобы масштаб был по-настоящему лёгким, я бы добавил ещё 3 “микро-фичи инфраструктуры” (это 1–2 дня работы, но окупается):
	1.	InboundRoute (маппинг входящих событий на ресторан)
	2.	Message.type + уникальность (lead, type) (дедуп на SMS)
	3.	Индексы + pagination policy (чтобы не умереть от объёма данных)

Если хочешь — я могу прямо сейчас написать добавочный mini-plan (ещё 10 микрошагов), которые делают MVP “готовым к 100 ресторанам” без изменения продукта.


Коротко: да, соответствует базовому тарифу — при условии, что базовый тариф = “AI отвечает + сохраняем лиды + отправляем SMS/ссылку”.

Что покрывает MVP (Basic):
	•	✅ AI отвечает на звонки (через Retell AI)
	•	✅ Сохраняются звонки и транскрипты
	•	✅ Создаются лиды
	•	✅ Отправляется SMS recap (через Twilio)
	•	✅ Отправляется ссылка на бронирование (например, OpenTable)
	•	✅ Есть админка с историей звонков

Что НЕ входит (и правильно для Basic):
	•	❌ Проверка availability
	•	❌ Follow-up через 20 минут
	•	❌ Reminders / no-show
	•	❌ WhatsApp
	•	❌ Revenue tracking

⸻

Вывод:
Этот MVP полностью покрывает логичный Basic план:
“Не теряем звонки + получаем контакты + отправляем ссылку на бронь”.

Если хочешь, могу коротко сказать, чего минимально не хватает, чтобы Basic выглядел “дороже”, но почти без дополнительной разработки.


Коротко и по делу — вот список того, что нужно добавить поверх MVP, чтобы появился второй тариф (Conversion / Inteligente).

Без availability API. Только то, что реально увеличивает конверсию.

⸻

🥈 Второй тариф — что нужно внедрить

1️⃣ Reservation Link Tracking

Цель: понять, кто реально перешёл по ссылке.

Нужно сделать:
	•	Таблица TrackingEvent
	•	Генерация уникальной ссылки:

/r/<tracking_id>


	•	Endpoint redirect → 302 → OpenTable/Resy
	•	Логирование клика

⸻

2️⃣ Follow-up если не кликнули

Цель: увеличить завершённые бронирования.

Нужно:
	•	Celery задача “check_reservation_click”
	•	Таймер 20–30 минут после первого SMS
	•	Если нет TrackingEvent → отправить reminder SMS
	•	Дедупликация (не больше 1 follow-up)

⸻

3️⃣ Intent Accuracy Improvement

Чтобы тариф выглядел умнее.
	•	Улучшить маппинг intent (reservation detection)
	•	Фиксировать party_size и requested_datetime (если есть)
	•	Отображать это в админке

⸻

4️⃣ Overflow Mode (если не ответили)

Это сильная фича для тарифа 2.

Нужно:
	•	Настройка в Restaurant: overflow_enabled
	•	Twilio routing logic (если не ответили X секунд → Retell)
	•	Флаг в админке

(Архитектурно не сложно, но потребует настройки Twilio)

⸻

5️⃣ Revenue Estimation (первая версия)

Не реальная выручка, а estimation.
	•	В Restaurant добавить average_ticket_value
	•	В админке считать:

reservation_intents × average_ticket


	•	Показать “Estimated Revenue Generated”

Это дешёво в разработке, но сильно повышает ценность.

⸻

6️⃣ Базовые KPI в админке

Добавить блок статистики:
	•	Reservation intents
	•	Link clicks
	•	Conversion rate (click / intent)
	•	Avg call duration

⸻

7️⃣ Message Type + Дедупликация

Чтобы не было хаоса:
	•	В Message добавить message_type
	•	recap
	•	reservation_link
	•	follow_up
	•	Уникальность (lead, message_type)

⸻

Итог

Чтобы включить тариф 2, нужно внедрить:
	1.	Tracking links
	2.	Follow-up automation
	3.	Revenue estimation
	4.	KPI dashboard
	5.	(Опционально, но сильно) Overflow mode

⸻

Что НЕ нужно для тарифа 2
	•	❌ OpenTable API availability
	•	❌ Booking creation API
	•	❌ Reminders 24h/3h
	•	❌ WhatsApp
	•	❌ CRM tagging

Это всё уже третий тариф.

⸻

Если хочешь, могу следующим шагом:
	•	Разбить тариф 2 на такие же микрошаги, как мы делали с MVP
или
	•	Оценить сложность и время внедрения каждого пункта.